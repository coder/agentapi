# DEPRECATED

This directory is no longer in use.

## Status

**Deprecated** - This Go library is no longer maintained and has been superseded by the TypeScript implementation in `chat/src/`.

## Contents

| Module | Description |
|--------|-------------|
| `httpapi` | HTTP API server handlers |
| `logctx` | Logging context utilities |
| `msgfmt` | Message formatting utilities |
| `screentracker` | Terminal screen tracking |
| `termexec` | Terminal execution |
| `util` | General utilities |

## Migration

All functionality has been migrated to the TypeScript codebase under `chat/src/`. No migration steps are required as this directory is no longer referenced by any code in the repository.

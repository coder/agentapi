name: Journey Gate
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  journey-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Journey gate: stub"

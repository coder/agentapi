repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
        args: ["--maxkb=500"]
      - id: detect-private-key

# Local security and fast gate hooks for pre-commit and pre-push stages
  - repo: local
    hooks:
      - id: security-guard-pre-commit
        name: security-guard pre-commit
        entry: .github/scripts/security-guard.sh pre-commit
        language: script
        pass_filenames: false
        stages: [pre-commit]
      - id: security-guard-pre-push
        name: security-guard pre-push
        entry: .github/scripts/security-guard.sh pre-push
        language: script
        pass_filenames: false
        stages: [pre-push]

  - repo: https://github.com/returntocorp/semgrep
    rev: v1.45.0
    hooks:
      - id: semgrep
        args: ['--config=.semgrep-rules/', '--error']

  - repo: https://github.com/trufflesecurity/trufflehog
    rev: v3.63.0
    hooks:
      - id: trufflehog
        args: ['filesystem', '.', '--only-verified', '--fail']
